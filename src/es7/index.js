let numbers = [1, 2, 3, 4, 5, 6, 7];
//con includes podemos buscar un valor que este en el array
if (numbers.includes(9)) {
  console.log('si se encuentra el valor 7');
} else {
  console.log('no se encuentra');
}
//con dos ** podemos elevar a la potencia
let base = 4;
let exponent = 3;
let result = base ** exponent;
console.log(result);